<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    tools:context=".RegistrActivity">

    <LinearLayout
        android:orientation="vertical"
        android:background="@drawable/rounded_rect_white"
        style="@style/BasicCanvas.Wrap">
        <TextView style="@style/TextView" android:text="Ваше полное Имя:" />
        <EditText style="@style/EditText" android:id="@+id/full_name" />
        <TextView style="@style/TextView" android:text="Ваш новый логин:" />
        <EditText style="@style/EditText" android:id="@+id/login" />
        <TextView style="@style/TextView" android:text="Ваш новый пароль:" />
        <EditText style="@style/EditText" android:id="@+id/password" android:inputType="textPassword" />
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <Button style="@style/Button.Accent"
                android:id="@+id/registration_button"
                android:layout_centerHorizontal="true"
                android:text="Зарегистрироваться" />
        </RelativeLayout>
    </LinearLayout>

</android.support.constraint.ConstraintLayout>